/*
    Решения:

        1)  Запустите её, перенаправив поток ввода из файла numbers.txt. Отсортированные числа должны напечататься на экран.

            ./a.out < numbers.txt     (Linux)               a.exe < numbers.txt     (Windows)


        2)  Запустите программу, перенаправив поток ввода из файла numbers.txt и перенаправив поток вывода
            в файл sorted.txt. Отсортированные числа должны сохранится в новом файле sorted.txt.

            ./a.out < numbers.txt > sorted.txt     (Linux)               a.exe < numbers.txt > sorted.txt     (Windows)
            
*/
