/*
    Задача:

        В этой программе мы создали 4 переменные, которые имеют типы int, size_t, char и double соответсвенно.
        Ваша задача заключается в том, чтобы напечатать:

            1)  Размеры каждой из этих переменных в байтах
            2)  Адрес каждой из этих переменных в 16-ричной системе счисления
*/

#include <stdio.h>


int main()
{
    int a;
    size_t b;
    char c;
    double d;
}