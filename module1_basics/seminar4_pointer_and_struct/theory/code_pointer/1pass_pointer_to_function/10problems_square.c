#include <stdio.h>


int main()
{

}


/*
    Задача:

        Напишите функцию square, которая будет принимать число a типа int и указатель p, который хранит
        адрес другой переменной типа int.
        Функция должна возводить число a в квадрат и записывать результат по адресу p.
        Функция не должна ничего возвращать.

        Вызовите эту функцию из функции main.
*/