#include <stdio.h>


int main()
{

}


/*
    Задача:

        Напишите функцию print, которая будет указатель типа int*
        
        Эта функция должна печатать на экран число, на которое указывает указатель.
        Но если указатель нулевой, то функция должна печтать на экран "Null pointer"

        Функция не должна ничего возвращать.
        Вызовите эту функцию из функции main.
*/